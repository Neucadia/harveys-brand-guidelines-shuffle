---
category: Brand
---

Closing developer section on the deep-green band: copyable CLI blocks for adding the Harvey's Design Spec (DESIGN.md) to a project's AI tooling — Claude plugin install first, then Gemini/Codex snapshot paths, then the retheme prompt — plus usage guidance (skill auto-loads; commit the project-scoped .claude/settings.json), the update procedure (marketplace update pulls master; brand-sync for vendored code), and access fine print.
