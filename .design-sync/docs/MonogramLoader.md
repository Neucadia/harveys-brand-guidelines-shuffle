---
category: UI
---

The brand loader: H monogram breathing inside a spinning ring (sanctioned circle primitive). size: button | sm | md | lg; label, showLabel, inline. Self-contained — keyframes come from the Tailwind config; respects prefers-reduced-motion. Also exports MonogramLoaderMark and MonogramNegativeMark — the static negative lockup (solid egg, knockout H, currentColor fill) that NotFound renders in deep forest. Demo in CataloguePatterns.
