---
category: UI
---

Section opener: eyebrow chip over a centered uppercase headline plus optional gray lede. Props: eyebrow, title, lede, level (h1|h2), emphasis (ember chip — rationed, max one per screen). Wheat chip by default. Demo in CataloguePatterns.
