---
category: UI
---

The brand loader: H monogram breathing inside a spinning ring (sanctioned circle primitive). size: button | sm | md | lg; label, showLabel, inline. Self-contained — keyframes come from the Tailwind config; respects prefers-reduced-motion. Also exports MonogramLoaderMark, the static MonogramMark, and MonogramNegativeMark (solid currentColor disc + cream mark — the negative lockup NotFound uses in deep forest). Demo in CataloguePatterns.
