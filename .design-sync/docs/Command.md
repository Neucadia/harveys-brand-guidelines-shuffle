---
category: UI
---

cmdk palette: hairline-bordered list, uppercase seed-tag group headings, cream + green selection. CommandDialog wraps it in the Dialog. Compose Command + CommandInput / CommandList / CommandGroup / CommandItem / CommandEmpty / CommandSeparator / CommandShortcut.
